node.name: "{{ hostname }}"
cluster.name: "{{ es_cluster_name | mandatory }}"
network.host: "{{ es_listen_addr }}"
network.bind_host: "{{ es_listen_addr }}"
network.publish_host: "{{ ansible_local.tinc.vpn_ip }}"

# minimum_master_nodes need to be explicitly set when bound on a public IP
discovery.zen.ping.unicast.hosts: {{ es_master_nodes_and_ports | to_yaml }}
discovery.zen.minimum_master_nodes: {{ es_min_master_nodes }}

transport.profiles.default.port: {{ es_node_port | mandatory }}
